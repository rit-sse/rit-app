import { View } from "react-native";

export default function RecentlyViewedButton() {
    return (
        <View style={{width: 65, height: 65, backgroundColor: "rgba(0,0,0,0.1)", marginRight: 14, borderRadius: 5}}>

        </View>
    );
}